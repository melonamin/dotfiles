complete -c devcon -a "(ls -1 $HOME/devcontainers)" -d "Container template"
complete -c devcon-up -a "(ls -1 $HOME/.devcontainers)" -d "Container template"
complete -c devcon-create -x -d "Create new template"
