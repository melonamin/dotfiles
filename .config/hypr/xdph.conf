screencopy {
    allow_token_by_default = true
    custom_picker_binary = hyprland-preview-share-picker
    # Embed the cursor in captures; apps like Chrome don't request a cursor
    # mode, and the portal's fallback is HIDDEN, which loses the cursor in
    # window shares (screen shares survive via the software cursor).
    cursor_mode = 2
}
